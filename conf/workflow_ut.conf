{
    "workflow": "ut", 
    "type": "sql",
 
    "scripts": [
        {"script": "ut_start.sql",          "comment": "create the result table"},
        {"script": "ut_basic_gen.sql",      "comment": "automatically created tests"},
        {"script": "ut_care_site.sql",      "comment": ""},
        {"script": "ut_person.sql",         "comment": ""}

    ]
}
